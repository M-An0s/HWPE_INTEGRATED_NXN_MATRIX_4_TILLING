  /*
        for (int i = 0;i<2;i++){
            ap_uint<64> word = buffer_1[i];
            ap_uint<16> v0 = word.range(15,0);    // a
            ap_uint<16> v1 = word.range(31,16);  // a
            ap_uint<16> v2 = word.range(47,32);  // b
            ap_uint<16> v3 = word.range(63,48);  // b
            REG1[i] = word;
            sum += v0*v2 + v1*v3;
            }
buffer_1[0] = sum; 

/*
int w = 1;
read_and_write_back:
    for (int out = 0; out < Size2; out++) {
        #pragma HLS PIPELINE II=2
        
        res_t sum = 0;
        if(out == 0){
            for (int i = 0;i<2;i++){
                ap_uint<64> word = buffer_1[i];
                ap_uint<16> v0 = word.range(15,0);    // a
                ap_uint<16> v1 = word.range(31,16);  // a
                ap_uint<16> v2 = word.range(47,32);  // b
                ap_uint<16> v3 = word.range(63,48);  // b
                REG1[i] = word;
                sum += v0*v2 + v1*v3;
            }
            buffer_1[out] = sum; 
        }
        else{
       ap_uint<64> word = buffer_1[w];
        ap_uint<8> v0 = word.range(7,0);    // a
        ap_uint<8> v1 = word.range(23,16);  // a
        ap_uint<8> v2 = word.range(39,32);  // b
        ap_uint<8> v3 = word.range(55,48);  // b
        buffer_1[w++] = (v3, v2, v1, v0);}
         //mix small value a and b into the 32 bit number 
        /*res_t sum = 0;
        //brin first row first col 
        for (int s = 0; s < N; s++) {
            ap_uint<64> word = buffer_1[w++];
            ap_int<32> a = word.range(31, 0);
            ap_int<32> b = word.range(63, 32);
            sum += (res_t)a * (res_t)b;
        }
        buffer_1[out] = sum;
    }
    // returns -> ap_done pulses */